{
  "name": "edsoncarlosdevops-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website — Hugo + GitHub Pages",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.4",
    "postcss-cli": "^11.0.1"
  }
}
